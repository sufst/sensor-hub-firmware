{# Template for sensor_hub.h (ignore the below, edit THIS file!) #}
/* Auto-generated by generate.py — do not edit manually. */
#ifndef SENSOR_HUB_H
#define SENSOR_HUB_H

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/* CAN message IDs */
{% for msg in all_messages %}
#define {{ msg.id_define }}  {{ msg.can_id_hex }}U
{% endfor %}

/* CAN message DLCs (bytes) */
{% for msg in all_messages %}
#define {{ msg.dlc_define }}  {{ msg.dlc }}U
{% endfor %}

/* Board-agnostic I2C aliases used by SUFST/Src/sensors.c */
{% for msg in i2c_groups %}
#define SENSOR_HUB_{{ msg.name | replace(ecu_name ~ '_', '') }}_ID   {{ msg.id_define }}
#define SENSOR_HUB_{{ msg.name | replace(ecu_name ~ '_', '') }}_DLC  {{ msg.dlc_define }}
{% endfor %}

void SensorHub_Init(void);
void SensorHub_Transmit(void);

#ifdef __cplusplus
}
#endif

#endif /* SENSOR_HUB_H */
